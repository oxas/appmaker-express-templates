{
	"name": "appmaker-express-templates",
	"version": "1.0.0",
	"description": "Run the following command to install @oxas/appmaker globally:",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"glob": "latest"
	},
	"devDependencies": {
		"@types/node": "latest"
	}
}
